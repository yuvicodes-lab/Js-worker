{
  "name": "arcpanel-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute arcpanel --file=./migrations/001_init.sql"
  },
  "dependencies": {
    "jose": "^5.2.0"
  }
}